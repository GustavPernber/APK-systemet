const Divider = (props: {className?: string}) => {
    return (
        <span
        className=" w-full h-[2px] bg-gray-200 rounded-full"
        {...props}
        />
    )
}

export default Divider