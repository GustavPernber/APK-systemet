name: 'build'

on: [push]

jobs:

  Build:
      runs-on: ubuntu-latest
      
      steps: 
        - name: Checkout code.
          uses: actions/checkout@v2
        - run: npm ci
        - run: npm run build